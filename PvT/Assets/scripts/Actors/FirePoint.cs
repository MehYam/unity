using UnityEngine;
using System.Collections;

public sealed class FirePoint : MonoBehaviour
{
    //KAI: we're probably not using this stuff
    public enum TYPE { LASER, ROCKET, SHIELD, BULLET };
    public TYPE type;
}
